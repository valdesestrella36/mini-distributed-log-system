[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-distributed-log-broker"
version = "0.1.0"
description = "Mini distributed log broker (MVP)"
authors = [ { name = "Author" } ]
dependencies = []
